
# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli
.expo/

# Native prebuild output (regenerate with: npx expo prebuild)
/android

# FCM config — referenced by app.json googleServicesFile, kept out of git.
# NOTE: .easignore deliberately re-includes this so EAS local builds can copy
# it during prebuild (which regenerates /android).
google-services.json

# Local build output (APKs from `eas build --local`)
/build
